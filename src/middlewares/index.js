const verifyDescription = require('./verifyDescription');
const verifyStatus = require('./verifyStatus');
const verifyTask = require('./verifyTask');
const verifyTitle = require('./verifyTitle');
const verifyToken = require('./verifyToken');

module.exports = {
  verifyDescription,
  verifyStatus,
  verifyTask,
  verifyTitle,
  verifyToken,
};
